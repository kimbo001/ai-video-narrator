// prisma.config.ts
import { defineConfig, env } from 'prisma/config';

export default defineConfig({
  schema: 'prisma/schema.prisma',
  datasource: {
    // This tells Prisma to read the URL from your .env file
    url: env('DATABASE_URL'),
  },
});
