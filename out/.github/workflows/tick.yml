name: Hourly Counter Bump

on:
  schedule:
    - cron: '0 * * * *'   # every hour UTC
  workflow_dispatch:      # manual trigger button

jobs:
  bump:
    runs-on: ubuntu-latest
    steps:
      - name: Hit bump endpoint
        run: |
          curl -X POST https://aivideonarrator.com/api/tick
